'use strict';

/**
 * @ngdoc function
 * @name nodeChatroomApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the nodeChatroomApp
 */
angular.module('nodeChatroomApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
